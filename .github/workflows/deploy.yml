name: Deploy

on:
  push:
    branches: [main]
    # Don't run deploy on push to docs/ or any markdown-only edit;
    # only deploy when source or assets actually changed.
    paths:
      - 'src/**'
      - 'public/**'
      - 'scripts/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'wrangler.toml'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Cloudflare Pages environment to deploy to'
        required: true
        default: production
        type: choice
        options:
          - production
          - preview

# Cancel in-progress runs on the same branch when a new commit lands.
concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    name: Build & verify
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      # pnpm version comes from package.json `packageManager` field — don't
      # also pass `version:` here or the action errors with "Multiple versions
      # of pnpm specified".
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # prerender.mjs drives a headless Chromium via Playwright. The package
      # itself is already installed by pnpm above, but the browser binary
      # isn't. Cache it under ~/.cache/ms-playwright so this stays fast.
      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Typecheck
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Build (vite + prerender)
        env:
          PUBLIC_SITE_URL: ${{ vars.PUBLIC_SITE_URL }}
        run: pnpm build

      - name: Verify build output
        run: |
          set -euo pipefail
          test -f dist/client/index.html
          for r in zh zh/privacy en en/privacy; do
            test -f "dist/client/$r/index.html" || { echo "MISSING: dist/client/$r/index.html"; exit 1; }
          done
          test -f dist/client/favicon.png
          test -f dist/client/sitemap.xml
          echo "✓ build output structure OK"

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/client

  deploy:
    name: Deploy to Cloudflare Pages
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 10

    # `environment` provides protection rules + secrets scoping on GitHub.
    environment:
      name: ${{ inputs.environment || 'production' }}
      url: ${{ steps.deploy.outputs.deployment-url }}

    steps:
      # Ensure the Pages project exists. cloudflare/pages-action fails with
      # "Project not found" if the project was never created in the
      # Cloudflare dashboard. `wrangler pages project create` is idempotent
      # — re-running on an existing project returns 409, which we ignore.
      - name: Ensure Pages project exists
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: dist/client
          command: >-
            pages project create novaview-com
            --production-branch=main
            --compatibility-date=2026-06-12
            || echo "::warning::project create returned non-zero (likely already exists)"
          quiet: false

      - name: Deploy to Cloudflare Pages
        id: deploy
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: novaview-com
          directory: dist/client
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
          wranglerVersion: '3'

  lighthouse:
    name: Lighthouse mobile audit
    needs: deploy
    # Only run on production pushes (not manual previews). Keep it light
    # to stay under free-tier Cloudflare build minutes.
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with: { node-version: '20', cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - name: Lighthouse mobile (a11y + SEO + perf budget)
        uses: treosh/lighthouse-ci-action@v11
        with:
          configPath: .lighthouserc.json
          uploadArtifacts: true
          temporaryPublicStorage: true
